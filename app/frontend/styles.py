"""Small UI helper.

The application intentionally relies on native Streamlit components so that
light/dark mode is handled by Streamlit instead of hard-coded CSS colours.
"""


def apply_styles():
    # Kept for compatibility with streamlit_app.py.
    # No custom CSS is required for the current interface.
    return None
